import React from "react";
import { Layout } from "components/common";
import { Intro, Contact, Skills} from "components/landing";

export default () => (
  <Layout>
    <Intro />
    <Contact />
    <Skills />

  </Layout>
);
